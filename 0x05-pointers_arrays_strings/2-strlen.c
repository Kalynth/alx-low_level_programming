#include "main.h"
/**
 * _strlen -  returns the length of a string
 * @s: String
 * Return: Length of string
 */
int _strlen(char *s)
{
	char *skl;
	int len;

	skl = "Hello Alex";
	len = _strlen(skl);
	return (0);
}
